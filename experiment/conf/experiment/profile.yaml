# @package _global_
# Profiling Configuration (Minimal 1-step execution)

defaults:
  - base
  - _self_

experiment:
  name: "profiling_run"
  type: "evaluation"
  description: "Minimal 1-step run for profiling"

# Override execution for 1-step profiling
execution:
  duration_sec: 5  # 1 step at 100Hz = 0.01 seconds
