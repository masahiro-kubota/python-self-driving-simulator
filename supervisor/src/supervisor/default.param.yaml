# Supervisor Default Parameters

# Goal Configuration
goal_x: null  # Goal X coordinate [m] (null means no goal check)
goal_y: null  # Goal Y coordinate [m] (null means no goal check)
goal_radius: 5.0  # Goal threshold [m]
min_elapsed_time: 20.0  # Minimum elapsed time [s] before goal check

# Timeout Configuration
max_steps: null  # Maximum number of steps (null means infinite or determined by runner)
